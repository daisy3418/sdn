from agent.llm_agent import agent_parse_intent


tests = [
    "配置服务器区保护策略",
    "按照最小权限原则配置当前内网",
    "保护管理安全区，禁止异常主机访问管理员主机和监控审计主机",
    "保护服务器区，同时保留管理员访问权限",
    "普通用户只能访问 Web服务器，不能直接访问数据库",
]

for text in tests:
    print("User intent:")
    print(text)

    rules = agent_parse_intent(text)

    print("LLM parsed rules:")
    print(rules)
    print("-" * 50)